module.exports = async (client) => {
    //const logChannelID = "1200570796159021176";
    //const logChannel = await client.channels.fetch(logChannelID);
    //logChannel.send(`${client.user.tag} is online with no startup issues.`);
};